{{- if .Values.volumes.libraries.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "archive.fullname" . }}-libraries-pvc
  labels:
    {{- include "archive.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - {{ .Values.volumes.libraries.accessMode }}
  resources:
    requests:
      storage: {{ .Values.volumes.libraries.size }}
  {{- if .Values.volumes.libraries.storageClass }}
  storageClassName: {{ .Values.volumes.libraries.storageClass }}
  {{- end }}
{{- end }}

